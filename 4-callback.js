//setTimeout is a nodejs api
setTimeout(() => {
    console.log('Two seconds are up');
}, 2000)

//filter javascript
const names = ['Kiran','Arun','Ali']
const shortNames = names.filter((names) => {
    return names.length <= 4;
})

const geocode= (address, callback) => {
    setTimeout(() => {

    }, 2000)
}

geocode('philadelphia', );
